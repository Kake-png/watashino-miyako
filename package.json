{
  "name": "ekimachi-zufu-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/validate.mjs",
    "photos:fetch": "node scripts/fetch-commons-photos.mjs",
    "deploy": "npx wrangler pages deploy public"
  },
  "dependencies": {
    "maplibre-gl": "5.24.0"
  }
}
